# Зависимости generate_dataset.py. Установка из локального wheelhouse (офлайн) допустима:
#   pip install --no-index --find-links /path/to/wheels -r requirements.txt
numpy>=1.24,<3
pyarrow>=14.0
pillow>=10.0
opencv-python-headless>=4.8
av>=11.0
PyYAML>=6.0
# Только для запуска тестов (src/tests), генератору не нужен:
# pytest>=7.4
