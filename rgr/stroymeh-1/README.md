# Строительная механика. Задание 1, вариант 2

Источник: МГСУ, кафедра строительной механики. «Расчёт статически определимых
стержневых систем». Задания для студентов бакалавриата. — М., 2015.
Схемы варианта 2 — с. 6, числовые данные — таблица 1, с. 4.

Сделано: кинематический анализ, опорные реакции и эпюры внутренних усилий
M, Q, N для всех пяти схем варианта. Схемы перечерчены заново с правками
направлений нагрузок, внесёнными от руки в оригинал.

## Исходные данные (таблица 1, строка 2)

| l, м | h, м | q, кН/м | F, кН | m, кН·м |
|---|---|---|---|---|
| 6 | 4 | 1 | 20 | 10 |

Номер строки таблицы преподаватель задаёт отдельно от номера варианта; здесь
принята строка 2 по номеру варианта — это нужно подтвердить.

## Результат: опорные реакции

| схема | реакции |
|---|---|
| 1 | R_Ax = 4 кН; R_Ay = 20 кН (вниз); M_A = 242 кН·м |
| 2 | R₁ = 6 кН; R₂ = −3 кН; R₃ = −17 кН |
| 3 | R_1v = −25,33 кН; R_1h = 20 кН; R_2v = 31,33 кН; R_2h = −40 кН |
| 4 | R_S = −13,33 кН; R_Bx = 24 кН; R_By = 13,33 кН; M_B = 178 кН·м |
| 5 | R_Ay = 9 кН; M_A = 36 кН·м; R_B = 12,83 кН; R_C = 10,17 кН |

## Файлы

| файл | что это |
|---|---|
| `shablon.html` | шаблон страницы: `__CSS__`, схемы `__SH_n__`, эпюры `__EP_n_M/Q/N__` |
| `index.html` | собранная страница (публикуется как Artifact) |
| `_css.html` | стили страницы, схем и эпюр |
| `rama.py` | решатель: реакции и внутренние усилия в точной арифметике |
| `sistemy.py` | пять схем варианта — узлы, стержни, нагрузки, опоры, шарниры |
| `shemy.py` | библиотека черчения расчётных схем и сами пять схем |
| `epury.py` | построение эпюр: штриховка, контур, подписи ординат |
| `figury.py` | сборка рисунков — по рисунку на каждую эпюру |
| `ordinaty.py` | таблицы характерных ординат, экстремум M там, где Q = 0 |
| `reakcii.py` | вывод реакций схемы 1 с проверкой ΣM_K |
| `sborka-pdf.py` | сборка страницы, печатной версии и PDF |
| `shema-*.svg`, `epury-*.svg` | схемы и эпюры |
| `*.pdf` | собранная печатная версия |

## Как считалось

Всё — обыкновенными дробями (`fractions.Fraction`), без плавающей точки.
`rama.py` составляет ΣX, ΣY, ΣM плюс по уравнению на каждый шарнир и решает
систему методом Гаусса над дробями. Усилия в сечении считаются от дальней
отсечённой части, а `check_section` пересчитывает то же сечение от ближней:
**625 проверенных сечений, расхождений нет**. Дополнительно сходятся нулевые
моменты в шарнирах и на свободных концах и моменты в заделках — они совпадают
с найденными реакциями.

Правила знаков: эпюра M — со стороны растянутого волокна, без знака; на
эпюрах Q и N знак указан.

## Как перечерчивались схемы

Методичка отсканирована в двухцветном режиме (JBIG2), тонкие линии местами
потеряны. Страница варианта рендерилась в PNG через PyMuPDF с увеличением,
положения узлов, шарниров, опор и нагрузок снимались с растра попиксельно
(анализ тёмных пикселей по строкам и столбцам), после чего соотношения участков
сверялись с размерными засечками. Например, для схемы 5 засечки дали цепочку
l, l, l/2, l/2, l/2, l/2, l/2 — то есть полная длина 4,5·l.

Три места скан не разрешает, они перечислены в разделе «Что в скане не читается»
самого документа: горизонтальные размеры схемы 2, точное положение момента m
на схеме 2 и положение точки K на схеме 4.

## Пересборка

```bash
python3 shemy.py            # перечертить схемы
python3 figury.py           # построить эпюры
python3 ../obrezka-svg.py . # обрезать viewBox по содержимому
python3 sborka-pdf.py       # index.html + print.html + PDF
```

`../obrezka-svg.py` открывает рисунки в Chromium, берёт `svg.getBBox()`
и переписывает `viewBox` по фактическому содержимому. Без этого по краям
остаётся пустое поле: на экране незаметно, а в PDF рисунок из-за него
выходит мельче. Запускать после `shemy.py` и `figury.py`.
